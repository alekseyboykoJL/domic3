li
    a(href='#{curPath + crs.id}') #{crs.name}
    button(class="btn btn-link", data-toggle="modal", data-target="", aligin="left")
      a(title="edit1", class="glyphicon glyphicon-pencil", href="#{curPath + crs.id +'/edit'}")